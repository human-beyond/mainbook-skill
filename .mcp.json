{
  "mcpServers": {
    "mainbook": {
      "command": "uvx",
      "args": [
        "mainbook-mcp",
        "${HOME}/Downloads",
        "${HOME}/Desktop",
        "${HOME}/Documents"
      ]
    }
  }
}
